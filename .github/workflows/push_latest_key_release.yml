name: Push Latest Key Release Image

on:
  push:
    branches:
      - main
    paths:
      - examples/key_release/**
  workflow_dispatch:

jobs:
  push-latest-key-release:
    name: "Push Latest Key Release Image"
    uses: ./.github/workflows/build_and_push_images.yml
    secrets: inherit
    with:
      id: latest
      test: key_release
