name: Run All Tests

on:
  schedule:
    - cron: "0 9 * * *"
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  run-simple-server:
    name: Run All Tests
    uses: ./.github/workflows/run_test.yml
    secrets: inherit
    with:
      test: simple_server
