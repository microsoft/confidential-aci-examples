name: Run All Tests

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  run-simple-server:
    uses: ./.github/workflows/end_to_end_test.yml
    secrets: inherit
    strategy:
      matrix:
        test: [simple_server]
    with:
      test: ${{ matrix.test }}
