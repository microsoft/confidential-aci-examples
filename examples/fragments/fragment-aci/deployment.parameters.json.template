{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "name": { "value": "$CG_NAME" },
        "location": { "value": "$LOCATION" },
        "registry": { "value": "$REGISTRY" },
        "image": { "value": "$IMAGE" },
        "managedIDName": { "value": "$MANAGED_IDENTITY" }
    }
}
